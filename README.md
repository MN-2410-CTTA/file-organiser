# File Organizer

Organizes files in a folder into subfolders by type.

## Usage
1. Run `file_organizer.py`
2. Enter the folder path
3. Files are automatically moved into folders by type

## Features
- Handles text, images, PDFs
- Creates folders automatically
- Shows summary of files moved
- Error handling included
